import { Body, Controller, Get, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { SearchService } from './search.service'; import { RouteSearchDto } from './dto/route-search.dto'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { CurrentUser } from '../common/decorators/current-user.decorator';
@ApiTags('search') @Controller('search') export class SearchController { constructor(private service: SearchService) {} @Post('route') @ApiBearerAuth() @UseGuards(JwtAuthGuard) route(@Body() dto: RouteSearchDto,@CurrentUser() user:any) { return this.service.route(dto,user.sub); } @Get('route') @ApiBearerAuth() @UseGuards(JwtAuthGuard) routeGet(@Body() dto: RouteSearchDto,@CurrentUser() user:any) { return this.service.route(dto,user.sub); } }
