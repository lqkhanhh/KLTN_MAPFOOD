import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { Restaurant, Review } from '../database/entities'; import { CreateReviewDto } from './dto';
@Injectable() export class ReviewsService { constructor(@InjectRepository(Review) private reviews:Repository<Review>,@InjectRepository(Restaurant) private restaurants:Repository<Restaurant>) {} async create(dto:CreateReviewDto,userId:string) { const r=await this.restaurants.findOneBy({id:dto.restaurantId}); if(!r) throw new NotFoundException('Không tìm thấy quán'); const review=await this.reviews.save(this.reviews.create({...dto,userId})); const stats=await this.reviews.createQueryBuilder('v').select('AVG(v.rating)','rating').addSelect('COUNT(*)','count').where('v.restaurantId=:id',{id:r.id}).getRawOne(); r.rating=Number(stats.rating);r.reviewCount=Number(stats.count);await this.restaurants.save(r);return review; } }
