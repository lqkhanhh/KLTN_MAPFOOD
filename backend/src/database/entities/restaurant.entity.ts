import { Column, CreateDateColumn, Entity, ManyToOne, OneToMany, PrimaryGeneratedColumn, UpdateDateColumn } from 'typeorm'; import { User } from './user.entity'; import { MenuItem } from './menu-item.entity'; import { Review } from './review.entity';
@Entity('restaurants') export class Restaurant { @PrimaryGeneratedColumn('uuid') id: string; @Column() name: string; @Column() address: string; @Column({ type: 'geography', spatialFeatureType: 'Point', srid: 4326 }) location: object; @Column({ default: '08:00-22:00' }) openingHours: string; @Column({ type: 'numeric', precision: 2, scale: 1, default: 0 }) rating: number; @Column({ default: 0 }) reviewCount: number; @ManyToOne(() => User, u => u.restaurants, { onDelete: 'CASCADE' }) owner: User; @Column() ownerId: string; @OneToMany(() => MenuItem, m => m.restaurant, { cascade: true }) menuItems: MenuItem[]; @OneToMany(() => Review, r => r.restaurant) reviews: Review[]; @CreateDateColumn() createdAt: Date; @UpdateDateColumn() updatedAt: Date; }
