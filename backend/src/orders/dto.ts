import { Type } from 'class-transformer'; import { IsArray, IsEnum, IsISO8601, IsNumber, IsString, ValidateNested } from 'class-validator'; import { OrderStatus, OrderType } from '../database/entities/order.entity';
export class OrderItemDto { @IsString() menuItemId: string; @IsNumber() quantity: number; } export class CreateOrderDto { @IsString() restaurantId: string; @IsEnum(OrderType) type: OrderType; @IsISO8601() pickupAt: string; @IsArray() @ValidateNested({each:true}) @Type(() => OrderItemDto) items: OrderItemDto[]; } export class UpdateOrderStatusDto { @IsEnum(OrderStatus) status: OrderStatus; }
