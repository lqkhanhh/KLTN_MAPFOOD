import { Body, Controller, Get, Param, Patch, Post, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { OrdersService } from './orders.service'; import { CreateOrderDto, UpdateOrderStatusDto } from './dto'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { CurrentUser } from '../common/decorators/current-user.decorator';
@ApiTags('orders') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('orders') export class OrdersController { constructor(private service:OrdersService) {} @Post() create(@Body() dto:CreateOrderDto,@CurrentUser() user:any){return this.service.create(dto,user.sub)} @Get(':id') one(@Param('id') id:string){return this.service.findOne(id)} @Patch(':id/status') status(@Param('id') id:string,@Body() dto:UpdateOrderStatusDto){return this.service.status(id,dto.status)} }
